# OSRS Skill Selector

This repository hosts a single GitHub Pages web page for marking OSRS skills on the skill tab image.

The page lets the user click each skill box to mark it with a status color and adjust the grid if needed.

The page stores its data in the browser with `localStorage`. That saved data includes the selected markers, the current default action, and the grid position and size for that browser.